<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
    <link rel ="stylesheet" href="/css/index.css" />
    <title>The Watch Shop</title>
</head>

<body>
    <div class="container">
        <nav>
            <header>
                <h1>The Watch Shop</h1>
            </header>
        </nav>

        <div class="watchShop">

            <% for(let watch of watches) { %>
            
                <div class="watch">
                    <div class="watch_image">
                        <img src="/images/<%= watch.image %>" alt="<%= watch.name %>"/>
                    </div>

                    <div class="watch_name">
                        <a href="/watch?id=<%= watch.id %>"> <%= watch.name %> </a>
                    </div>

                    <div class="watch_rating">

                        <% for(let index = 0; index < watch.stars; index++ ) { %>
                            <span class="fa fa-star checked"></span>
                        <% } %>

                        <% for(let index = 0; index < 5 - watch.stars; index++ ) { %>
                            <span class="fa fa-star"></span>
                            <% } %>
                    </div>

                    <div class="watch_price">
                        <p class="current_price"> $<%= watch.originalPrice %> </p>
                        <p class="old_price"> 

                            <% if (watch.hasDiscount){ %>
                                $<%= watch.originalPrice * (100- watch.discount) / 100 %>
           
                               <% }else { %>
                                   
                                   $<%= watch.originalPrice%>
                            <% } %>
                        
                        </p>  
                    </div>

                    <% if(watch.hasDiscount) { %>
                    <div class="discount_rate"><p> <%= watch.discount %> % off</p></div>
                    <% } %>
                </div>

            <% } %>

        </div>
    </div>
</body>
</html>